type IotDevice {
  id: UUID!
  owner: Account!
  name: String!
  live: Live
  accessKey: String
}

input IotDeviceCreation {
  name: String!
}

type Live {
  id: UUID!
  device: IotDevice!
  video: Video!
  title: String!
  viewerCnt: Int!
}
